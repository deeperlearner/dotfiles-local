- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    # append
    ~/.bashrc.d/.bashrc_local: bashrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [./sourceror.sh, Appending `source/Include ~/*.d` for exisiting files]
